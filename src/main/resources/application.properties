spring.application.name=Second-Task
server.port=9090
# Remove hardcoded Stripe key for security. Use environment variable instead.
stripe.secret.key=${STRIPE_SECRET_KEY}
spring.datasource.url=jdbc:mysql://localhost:3306/Assignment2
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

stripe.secret.key=${STRIPE_SECRET_KEY}

#stripe.webhook.secret=dummy_secret